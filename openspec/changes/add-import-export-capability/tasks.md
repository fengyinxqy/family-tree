## 1. Backup format and validation

- [x] 1.1 定义版本化 JSON 备份格式及共享类型，覆盖人物、关系、人物事件与导出元信息
- [x] 1.2 为导入 payload 增加结构校验、版本校验和引用完整性校验

## 2. Export service and API

- [x] 2.1 新增导出服务，按当前用户范围组装完整家谱备份数据
- [x] 2.2 新增导出 API，返回可下载的 JSON 备份内容与必要响应头

## 3. Import restore flow

- [x] 3.1 新增导入服务，按事务方式清空并重建当前用户的家谱核心数据
- [x] 3.2 在导入过程中处理人物新旧主键映射，确保关系和事件引用正确恢复
- [x] 3.3 新增导入 API，接收 JSON 备份并返回恢复结果摘要或校验错误

## 4. Entry points and verification

- [x] 4.1 为家谱工作台或设置区域补充导入导出入口与基础交互反馈
- [x] 4.2 为导入导出服务补充测试，覆盖成功导出、成功恢复、非法版本、断裂引用与事务回滚场景
- [x] 4.3 运行相关测试与静态检查，确认导入导出能力未破坏现有家谱维护链路
