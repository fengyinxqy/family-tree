## 1. 依赖与数据基础

- [x] 1.1 固定并安装兼容的 LangChain Core、LangGraph、模型适配器和 PostgreSQL 检查点依赖，记录所选版本及升级边界。
- [x] 1.2 在 Prisma 模式中新增 AgentSession、AgentMessage、AgentRun、AgentStep 和 AgentToolCall 实体、状态枚举、唯一活动运行约束及必要索引。
- [x] 1.3 生成并检查 Agent 业务表迁移，验证迁移不会修改现有人物、关系、修订、审计和资料数据。
- [x] 1.4 配置 LangGraph PostgreSQL 检查点存储及初始化流程，并以 AgentSession.id 作为 thread_id 验证写入和恢复。

## 2. LangChain 适配层

- [x] 2.1 实现统一 LangChain ChatModel 工厂，迁移现有 OpenAI/DeepSeek 配置并记录提供方、模型及调用用量元数据。
- [x] 2.2 定义版本化 Agent 状态、消息、预算、终止原因、人工介入和流式事件的 Zod 模式及 TypeScript 类型。
- [x] 2.3 实现提示词、图版本和工具模式版本注册机制，使每次 AgentRun 固化可追踪版本信息。
- [x] 2.4 为模型工厂增加契约测试，覆盖提供方选择、缺失配置、结构化输出、超时和错误分类。

## 3. 工具注册与安全执行

- [x] 3.1 实现统一工具注册表，要求每个工具声明名称、版本、输入输出模式、权限动作、超时和 READ/PROPOSE 副作用等级。
- [x] 3.2 实现工具执行器，在每次调用前重新解析用户与家谱权限，拒绝模型提供的作用域标识并校验输入输出。
- [x] 3.3 实现工具调用幂等键、安全参数/结果摘要、超时、错误分类和 AgentToolCall 审计持久化。
- [x] 3.4 将人物查询、关系查询、确定性亲属推理和资料缺口分析包装为 READ 工具，并添加模式与权限测试。
- [x] 3.5 将录入草稿和修订组创建包装为分离的草稿工具与 PROPOSE 工具，确保后者只能在有效确认后执行且不直接写正式记录。
- [x] 3.6 增加安全测试，证明发布、批准、撤回、删除和成员管理能力未注册且无法通过伪造工具名或参数调用。

## 4. 有界 LangGraph Runtime

- [x] 4.1 构建最小状态图，包含上下文加载、模型决策、工具执行、预算检查、人工介入、产物形成和终止节点。
- [x] 4.2 在每个模型和工具步骤后执行轮次、工具数、总耗时、重试和上下文预算检查，并持久化 BUDGET_EXHAUSTED 结果摘要。
- [x] 4.3 实现 AgentSession、AgentRun、AgentMessage 和 AgentStep 服务，保证同一会话最多一个活动运行并使用稳定步骤 ID 幂等写入。
- [x] 4.4 实现结构化 WAITING_FOR_USER 与 WAITING_FOR_CONFIRMATION 中断、检查点恢复及参数哈希、家谱修订号和过期时间校验。
- [x] 4.5 实现取消、失败分类和兼容检查点恢复；不兼容图状态必须保留历史并要求创建新运行。
- [x] 4.6 使用确定性测试模型和伪工具覆盖多工具循环、直接完成、预算耗尽、工具失败、重试、取消、并发启动和中断恢复。
- [x] 4.7 模拟工具完成后请求中断，验证恢复不会重复执行相同 PROPOSE 工具或创建重复修订组。

## 5. 资料补全 Agent

- [x] 5.1 定义资料补全目标范围、缺口分类、证据引用和报告/草稿产物模式，区分缺失、未发现、冲突与完整。
- [x] 5.2 构建资料补全子图，完成目标解析、范围受限调查、关键问题排序、报告生成和草稿生成流程。
- [x] 5.3 接入人物、关系、事件、资料引用和缺口工具，确保调查只访问当前家谱内授权且与目标相关的数据。
- [x] 5.4 实现多候选人物、关系方向不明、来源冲突和证据不足时的结构化人工澄清。
- [x] 5.5 将已确认草稿通过既有服务转换为不可变修订组，并验证人物、事件和关系在审校发布前保持不变。
- [x] 5.6 定义面向未来 OCR 的资料工具结果模式，覆盖资料/媒体标识、页码或区域、文本片段、置信度、提取器和模型版本。
- [x] 5.7 添加资料补全场景测试，覆盖范围限制、聚焦提问、未发现措辞、冲突证据、无新事实报告和确认后修订组。

## 6. API 与流式事件

- [x] 6.1 在编辑任何 Next.js 路由前阅读 node_modules/next/dist/docs 中与 Route Handlers、流式响应和运行时相关的当前指南，并记录采用的约定。
- [x] 6.2 新增 Agent 会话创建、列表和详情接口，使用集中式家谱授权并返回持久化消息、运行和介入状态。
- [x] 6.3 新增运行启动、恢复和取消接口，校验命令模式、活动运行冲突、确认绑定及状态转换。
- [x] 6.4 实现版本化 NDJSON 或等价流式事件协议，输出文本增量、步骤、工具、人工介入、产物、错误和终止事件。
- [x] 6.5 适配现有 `/api/agent/chat` 与新运行时，通过功能开关保留旧路径并确保回滚不依赖删除新数据。
- [x] 6.6 添加 API 测试，覆盖未登录、跨家谱访问、权限撤销、重复启动、断线后状态查询、恢复和取消。

## 7. Agent 客户端体验

- [x] 7.1 将 Agent 面板从纯前端消息状态迁移为持久化会话状态，并支持选择或继续历史会话。
- [x] 7.2 展示运行步骤、工具执行、预算终止、失败与取消状态，同时保持文本增量流式渲染。
- [x] 7.3 实现结构化澄清和确认界面，展示候选项、待执行动作摘要、过期或失效提示，并禁止重复提交。
- [x] 7.4 展示资料补全报告、来源依据和可审阅草稿，并复用既有修订组创建后的导航与状态反馈。
- [ ] 7.5 验证刷新、断线重连、重复点击、窄屏和长运行轨迹下的交互与可访问性。

## 8. 可观测性、文档与验收

- [x] 8.1 增加运行级结构化日志和指标，覆盖模型/图/工具版本、步骤耗时、Token、终止原因和错误分类，并验证敏感字段已脱敏。
- [x] 8.2 更新 AI 系统文档和环境变量示例，说明 LangChain/LangGraph 架构、检查点、预算、权限、人工介入、回滚和未来 OCR 扩展方式。
- [x] 8.3 编写迁移与运维说明，覆盖检查点初始化、功能开关、失败运行排查、版本不兼容和回滚流程。
- [x] 8.4 运行 Agent、权限、修订组、完整性、审计和 API 定向测试，确认 Agent 无法绕过正式审校发布工作流。
- [x] 8.5 运行 `npm run lint`、`npm run typecheck`、`npm test` 和 `npm run build`，修复本变更引入的所有失败。
