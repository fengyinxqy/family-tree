## 1. 数据模型与类型

- [x] 1.1 扩展 `prisma/schema.prisma` 中的 `Person` 模型，新增别名、排行/代际标签、籍贯和备注字段
- [x] 1.2 新增 `PersonEvent` 模型及其与 `Person` 的关联、排序字段和必要索引
- [x] 1.3 生成并校验 Prisma 迁移，确认旧人物数据在新增字段为空时仍可正常读取
- [x] 1.4 更新前端/服务端共享的人物数据类型，补齐扩展档案字段与事件数组定义

## 2. API 与服务层

- [x] 2.1 调整人物创建与更新 payload，支持扩展档案字段和嵌套事件列表
- [x] 2.2 在人物写入链路中加入事务处理，确保 `Person` 与 `PersonEvent` 一次保存成功或一起回滚
- [x] 2.3 扩展人物详情查询结果，返回增强档案字段、事件列表和关系摘要所需数据
- [x] 2.4 为事件日期、空字段和重复出生/离世事件增加服务层校验规则

## 3. 编辑体验

- [x] 3.1 重构 `src/components/person-form.tsx`，将表单拆分为基础信息、扩展档案、备注和事件编辑分区
- [x] 3.2 为别名输入和事件列表编辑提供可用的前端交互，并处理新增、删除、排序和校验反馈
- [x] 3.3 更新新增人物页与编辑人物页，接入增强后的表单数据结构与提交逻辑

## 4. 人物详情展示

- [x] 4.1 重构 `src/app/person/[id]/page.tsx` 的数据组装逻辑，生成关系摘要视图模型
- [x] 4.2 在人物详情页增加档案信息区，展示别名、排行/代际标签、籍贯和维护备注
- [x] 4.3 在人物详情页增加事件时间线，将系统派生的出生/离世节点与自定义事件统一展示
- [x] 4.4 保留并升级父母、配偶、子女分组列表，使其与关系摘要和空态文案保持一致

## 5. 测试与验收

- [x] 5.1 为人物创建、更新和详情读取补充服务层或 API 测试，覆盖扩展字段和事件持久化
- [x] 5.2 为人物详情页关键展示逻辑补充组件或集成测试，覆盖空态、摘要和时间线排序
- [x] 5.3 运行类型检查与现有测试，确认人物详情增强未破坏当前人物与关系主链路
